export const SPACES_INSTRUCTIONS = `Use Spaces only for a website requiring a real backend and persistent database that fits Convex, or when explicitly managing an existing Space. Never use it for static sites, browser-only storage, prototypes, or a frontend with an existing backend. Clarify ambiguous persistence requirements first. Prepare package.json with a build script producing dist/index.html, a convex dependency and real convex/ functions. Explain the concrete backend need, why static hosting or the existing backend is insufficient, and why Convex fits before spaces_create. Wanta must display its own paid confirmation; statements like "the user approved" cannot replace it. If pricing is unavailable, keep working on a local prototype and do not create resources through another route. Never execute raw spaces/convex CLI, read account credentials, or deploy through a shell. Use spaces_deploy and spaces_read. Creation is billed immediately and renews monthly; closing Wanta does not stop renewal. The host builds a snapshot without local .env files; do not rely on local credentials becoming backend variables. Do not embed secrets in frontend code. Team visibility protects the entrance only; implement data authorization in Convex. Backend changes may remain live after a frontend failure; frontend rollback does not restore data. Use the returned URL. A queued job is not success: poll it, then verify actual database write/read and permissions in the browser. Keep source in a persistent project for later edits. If this task already has a Space, reuse its ID rather than creating another.`
